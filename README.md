terraform-init AWS
===========

Initialize terraform configuration with separate remote states for every AWS service or project.
Requires AWS credentials for IAM FullAccess.

´´´
terraform init github.com/aguamala/terraform-init/aws

´´´
